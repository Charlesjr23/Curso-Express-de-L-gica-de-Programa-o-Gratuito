início {
    inteiro: n;
    inteiro:cont;
    inteiro: soma;
    real: media;
    cont=0;
    soma=0;
    
    enquanto(n >= 0){
        leia("digite o valor de n:" +cont, n);
        se(n >= 0){
            soma =(soma+n);
            cont=cont+1;
        }
    }
    media = soma/cont;
    escreva(media);
    
}